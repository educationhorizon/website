# Project Instructions

- You may inspect and edit files in this project when requested.
- Do not run `git commit`, `git pull`, or `git push`.
- Do not create commits or publish changes to GitHub unless the user explicitly changes this project instruction.
